// 导入vue 
import Vue from 'vue'
// 导入vue-router
import VueRouter from 'vue-router'
// 使用vue-router
Vue.use(VueRouter)
// 创建一个新的 vue-router对象
const router = new VueRouter({
    // 添加路由规则
    router:[
        
    ]
})
// 将新的对象暴露出去
export default router